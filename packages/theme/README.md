# @oas-isui/theme

OAS-ISUI 主题包 —— CSS 变量设计 token（light / dark / high-contrast 三套语义色、控件高度、字号、间距、圆角）。引入后 `html[data-theme="dark"]` 切换暗色。

```html
<link rel="stylesheet" href="https://unpkg.com/@oas-isui/theme@1/index.css" />
```

```ts
import '@oas-isui/theme'
```
